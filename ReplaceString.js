const qoute =
  "Next.js is one of the library in JS . The library work combine with JS all func.";

const convertOne = qoute.replace(/library/g, "Framework");
console.log(convertOne);

const convertwo = convertOne.replace(/JS/g, "React");
console.log(convertwo);